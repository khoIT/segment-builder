# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
**/coverage

# next.js
/.next/
/out/
dist/
**/dist/
bin/

# turbo
.turbo
**/.turbo

# production
/build

# misc
.DS_Store
Thumbs.db
*.pem

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# Claude design artifact exports (source of truth lives in src/)
*.zip
release-manifest.json

# Vite / build tool caches
.vite/
.cache/
.turbo/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# package manager — pnpm-lock.yaml is committed in monorepo
package-lock.json
yarn.lock
# pnpm-lock.yaml committed for reproducible installs across the monorepo

# semantic-release
.nyc_output

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# flutter
.dart_tool
build
GoogleService-Info.plist

repomix-output.xml
.serena/cache
plans/**/*
!plans/templates/*
screenshots/*
docs/screenshots/*
logs.txt
test-ck
__pycache__

# CK meta commands
prompt.md
ck.md

# Generated runtime layout for release/install smoke tests
/.claude/

# Local-only state inside tracked claude source
claude/agent-memory/
claude/hooks/.logs/
claude/session-state/latest.md
claude/session-state/archive/
claude/settings.bak.json
claude/skills/ai-multimodal/assets/demo-*.claude/session-state/*.tmp

# Gemini CLI settings (symlink to staged .claude/.mcp.json)
.gemini/settings.json
# External repos for study/reference
external/

# Git worktrees (local development only)
worktrees/

# Generated OpenCode backup from scripts/generate-opencode.py --force
.opencode.backup/
.opencode

# Stray root-level asset dirs from extracting "Segment Builder.zip" in-place
# (only needed when opening `LiveOps Engine.html` directly from the filesystem).
# Canonical copies that Vite serves live under public/.
/assets/
/fonts/
/uploads/
/screenshots/
*.tmp.txt

# demo runtime
.demo-drip.pid
.demo-drip.log

# stray dev logs
api*.log
